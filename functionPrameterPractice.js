function averageCalculator(a, b) {
    const sum = a + b;
    const ave = sum / 2;
    console.log(ave);
}
averageCalculator(20, 50);

function multipli(a, b) {
    const multi = a * b;
    console.log(multi);
}
multipli(20, 15)